naftiko: 1.0.0-alpha2
info:
  label: Fastly Products API — Image Optimizer
  description: 'Fastly Products API — Image Optimizer. 3 operations. Lead operation: Get Image Optimizer status. Self-contained
    Naftiko capability covering one Fastly business surface.'
  tags:
  - Fastly
  - Image Optimizer
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FASTLY_API_KEY: FASTLY_API_KEY
capability:
  consumes:
  - type: http
    namespace: products-image-optimizer
    baseUri: https://api.fastly.com
    description: Fastly Products API — Image Optimizer business capability. Self-contained, no shared references.
    resources:
    - name: enabled-products-v1-image_optimizer-services-service_id
      path: /enabled-products/v1/image_optimizer/services/{service_id}
      operations:
      - name: getimageoptimizerstatus
        method: GET
        description: Get Image Optimizer status
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: enableimageoptimizer
        method: PUT
        description: Enable Image Optimizer
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: disableimageoptimizer
        method: DELETE
        description: Disable Image Optimizer
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Fastly-Key
      value: '{{env.FASTLY_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: products-image-optimizer-rest
    port: 8080
    description: REST adapter for Fastly Products API — Image Optimizer. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/enabled-products/v1/image-optimizer/services/{service-id}
      name: enabled-products-v1-image-optimizer-services-service-id
      description: REST surface for enabled-products-v1-image_optimizer-services-service_id.
      operations:
      - method: GET
        name: getimageoptimizerstatus
        description: Get Image Optimizer status
        call: products-image-optimizer.getimageoptimizerstatus
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: enableimageoptimizer
        description: Enable Image Optimizer
        call: products-image-optimizer.enableimageoptimizer
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: disableimageoptimizer
        description: Disable Image Optimizer
        call: products-image-optimizer.disableimageoptimizer
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: products-image-optimizer-mcp
    port: 9090
    transport: http
    description: MCP adapter for Fastly Products API — Image Optimizer. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: get-image-optimizer-status
      description: Get Image Optimizer status
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: products-image-optimizer.getimageoptimizerstatus
      outputParameters:
      - type: object
        mapping: $.
    - name: enable-image-optimizer
      description: Enable Image Optimizer
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: products-image-optimizer.enableimageoptimizer
      outputParameters:
      - type: object
        mapping: $.
    - name: disable-image-optimizer
      description: Disable Image Optimizer
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: products-image-optimizer.disableimageoptimizer
      outputParameters:
      - type: object
        mapping: $.
